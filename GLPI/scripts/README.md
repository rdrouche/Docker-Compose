## Script de maintenance pour GLPI

## glpi-update.sh

Permet de mettre a jour la version de GLPI

Editer le script et modifier les variables suivantes :

*   `GLPI_VERSION_TARGET` : version de GLPI a installer
*   `GLPI_DOCKER_PATH` : emplacement où se trouve le fichier docker-compose.yml sur le serveur

> Avant toute mise à jour, effectuer une sauvegarde du serveur ou faire un snapshot