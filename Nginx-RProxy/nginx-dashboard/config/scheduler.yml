# version: 1.0.0
# Scheduler — Nginx Dashboard
# Cron format: "minute hour day month weekday"
# Examples:
#   "0 3 * * *"   = every day at 3:00
#   "0 */6 * * *" = every 6 hours
#   "30 2 * * 0"  = every Sunday at 2:30

nginx_reload:
  enable: false
  cron: "0 3 * * *"
  notify: true

backup:
  enable: false
  cron: "0 2 * * *"
  mode: local
  notify_on_failure: true
